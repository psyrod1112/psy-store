spring:
  application:
    name: psy-store
  profiles:
    default: local
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    issuer: psy-store
    access-token-ttl: 1h